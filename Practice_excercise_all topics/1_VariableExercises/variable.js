// 1. Declare a variable called `Name` and assign your name to it . 

const Name = `Deepak rajoriya`;

// 2. Create a variable called `age` and set your age as the value.

const age = 22;

// 3. Create a variable `isStudent` and set it to `true` or `false` based on your student status.

let isStudent = `true`;

// 4. Declare a variable `city` and assign the name of your city.

let city = `ujjain`;

// 5. Create a variable `favoriteColor` and set it to your favorite color.

let favoriteColor = `pink`;

// 6. Define a variable num1 and set it to a numerical value.

let num1 = 10;

// 7. Declare a variable `num2` and set it to a different numerical value.

let num2 = 20;

// 8. Calculate the sum of `num1` and `num2` and store it in a variable called sum.

let some = num1 + num2;

// 9. Calculate the difference between `num1` and `num2` and store it in a variable called difference.

let difference = num1 - num2;

// 10. Create a variable fullName by combining your name and age.Create a variable fullName by combining your name and age.

let fullName = Name + age;

// 11.  Declare a variable sentence and use string interpolation to create a sentence that includes your name and city.

let sentence = Name+city;

// 12. Define a variable isAdult and set it to true if your age is 18 or older, otherwise set it to false.

let isAdult = `true`;

// 13. Declare a variable randomNumber and assign a random integer between 1 and 100.

let randomNumber = 5;

// 14. Create a variable myNumber and set it to any numerical value.

const myNumber = 534022223;

// 15. Declare a variable myString and assign it a string value.

let myString = `jnfjgnrfgjgef`;

// 16. Define a variable isBoolean and set it to a boolean value (true or false).
 
let isBoolean = `true`;

